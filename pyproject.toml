[project]
name = "ax1200i"
version = "0.1.0"
description = "Python library for monitoring Corsair AX1200i PSUs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "liquidctl>=1.15.0",
    "pyserial>=3.5",
]
